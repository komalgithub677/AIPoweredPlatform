# Fullstack Auth Project

## Setup

### Backend
```bash
cd backend
npm install
cp .env.example .env
# edit .env with MongoDB URI & JWT_SECRET
npm run dev
```

### Frontend
```bash
cd frontend
npm install
npm start
```

By default, frontend runs on port 3000 and backend on 5000.
